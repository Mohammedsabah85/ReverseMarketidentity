@model StoresViewModel
@{
    ViewData["Title"] = "المتاجر";
}

<div class="container-custom">
    <!-- Header Section -->
    <div class="row mb-4">
        <div class="col-md-8">
            <h1 class="display-6 text-primary">
                <i class="fas fa-store me-3"></i>
                دليل المتاجر
            </h1>
            <p class="text-muted">تصفح المتاجر المسجلة في منصتنا</p>
        </div>
        <div class="col-md-4 text-end">
            <a href="/Account/Login" class="btn btn-outline-primary">
                <i class="fas fa-plus me-2"></i>
                سجل متجرك
            </a>
        </div>
    </div>

    <!-- Filters Section -->
    <div class="card-custom mb-4">
        <form method="get" class="row g-3">
            <div class="col-md-6">
                <label for="search" class="form-label">البحث</label>
                <input type="text" name="search" id="search" class="form-control"
                       placeholder="ابحث في اسم المتجر أو الوصف..." value="@Model.Search">
            </div>
            <div class="col-md-4">
                <label for="categoryId" class="form-label">الفئة</label>
                <select name="categoryId" id="categoryId" class="form-control">
                    <option value="">جميع الفئات</option>
                    @foreach (var category in Model.Categories)
                    {
                        if (Model.SelectedCategoryId == category.Id)
                        {
                            <option value="@category.Id" selected>@category.Name</option>
                        }
                        else
                        {
                            <option value="@category.Id">@category.Name</option>
                        }
                    }
                </select>
            </div>
            <div class="col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="fas fa-search me-1"></i>
                    بحث
                </button>
            </div>
        </form>
    </div>

    <!-- Results Section -->
    @if (Model.Stores.Any())
    {
        <div class="row g-4">
            @foreach (var store in Model.Stores)
            {
                <div class="col-lg-4 col-md-6">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body">
                            <div class="d-flex align-items-center mb-3">
                                @if (!string.IsNullOrEmpty(store.ProfileImage))
                                {
                                    <img src="@store.ProfileImage" class="rounded-circle me-3"
                                         style="width: 60px; height: 60px; object-fit: cover;" alt="@store.StoreName">
                                }
                                else
                                {
                                    <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center me-3"
                                         style="width: 60px; height: 60px;">
                                        <i class="fas fa-store"></i>
                                    </div>
                                }
                                <div>
                                    <h5 class="card-title mb-1">@store.StoreName</h5>
                                    <small class="text-muted">@store.FirstName @store.LastName</small>
                                </div>
                            </div>

                            @if (!string.IsNullOrEmpty(store.StoreDescription))
                            {
                                <p class="card-text text-muted">
                                    @(store.StoreDescription.Length > 100 ?
                                                            store.StoreDescription.Substring(0, 100) + "..." :
                                                            store.StoreDescription)
                    </p>
                                        }

                            <div class="mb-3">
                                @foreach (var storeCategory in store.StoreCategories.Take(3))
                                {
                                    <span class="badge bg-secondary me-1">@storeCategory.Category.Name</span>
                                }
                                @if (store.StoreCategories.Count > 3)
                                {
                                    <span class="badge bg-light text-dark">+@(store.StoreCategories.Count - 3)</span>
                                }
                            </div>

                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted">
                                    <i class="fas fa-map-marker-alt me-1"></i>
                                    @store.City - @store.District
                                </small>
                                <a href="/Stores/Details/@store.Id" class="btn btn-outline-primary btn-sm">
                                    عرض المتجر
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>

        <!-- Pagination -->
        @if (Model.TotalPages > 1)
        {
            <nav aria-label="pagination" class="mt-4">
                <ul class="pagination justify-content-center">
                    @if (Model.CurrentPage > 1)
                    {
                        <li class="page-item">
                            <a class="page-link" href="?page=@(Model.CurrentPage - 1)&search=@Model.Search&categoryId=@Model.SelectedCategoryId">
                                <i class="fas fa-chevron-right"></i>
                            </a>
                        </li>
                    }

                    @for (int i = Math.Max(1, Model.CurrentPage - 2); i <= Math.Min(Model.TotalPages, Model.CurrentPage + 2); i++)
                    {
                        <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                            <a class="page-link" href="?page=@i&search=@Model.Search&categoryId=@Model.SelectedCategoryId">@i</a>
                        </li>
                    }

                    @if (Model.CurrentPage < Model.TotalPages)
                    {
                        <li class="page-item">
                            <a class="page-link" href="?page=@(Model.CurrentPage + 1)&search=@Model.Search&categoryId=@Model.SelectedCategoryId">
                                <i class="fas fa-chevron-left"></i>
                            </a>
                        </li>
                    }
                </ul>
            </nav>
        }
    }
    else
    {
        <div class="text-center py-5">
            <i class="fas fa-store fa-4x text-muted mb-3"></i>
            <h4 class="text-muted">لا توجد متاجر</h4>
            <p class="text-muted">لم يتم العثور على متاجر تطابق معايير البحث</p>
            <a href="/Account/Login" class="btn btn-custom">
                <i class="fas fa-plus me-2"></i>
                سجل متجرك الآن
            </a>
        </div>
    }
</div>