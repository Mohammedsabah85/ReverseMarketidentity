@using Microsoft.Extensions.Localization
@using ReverseMarket.Services
@using ReverseMarket.Resources
@inject IStringLocalizer<SharedResource> Localizer
@inject ILanguageService LanguageService

@{
    var currentLanguage = LanguageService.GetCurrentLanguage();
    var direction = LanguageService.GetDirection();
    var supportedLanguages = LanguageService.GetSupportedLanguages();
    var currentLangInfo = supportedLanguages.FirstOrDefault(l => l.Code == currentLanguage) ?? supportedLanguages.First();

    var siteSettings = ViewBag.SiteSettings as ReverseMarket.Models.SiteSettings;

    // ✅ استخدام Identity بدلاً من Session
    var isLoggedIn = User.Identity?.IsAuthenticated ?? false;
    var userName = isLoggedIn ? (User.FindFirst(System.Security.Claims.ClaimTypes.GivenName)?.Value ?? User.Identity?.Name) : null;
    var isAdmin = isLoggedIn && User.IsInRole("Admin");
}

<!DOCTYPE html>
<html lang="@currentLanguage" dir="@direction">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="@Localizer["SiteDescription"]" />
    <meta name="keywords" content="سوق عكسي, طلبات, متاجر, العراق" />
    <meta name="author" content="@Localizer["SiteName"]" />

    <title>@ViewData["Title"] - @Localizer["SiteName"]</title>

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />

    <!-- Bootstrap CSS with RTL/LTR support -->
    @if (direction == "rtl")
    {
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css"
              rel="stylesheet"
              integrity="sha384-dpuaG1suU0eT09tx5plTaGMLBsfDLzUCCUXOY2j/LSvXYuG6Bqs43ALlhIqAJVRb"
              crossorigin="anonymous">
    }
    else
    {
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
              rel="stylesheet"
              integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
              crossorigin="anonymous">
    }


    <!-- Font Awesome Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
          rel="stylesheet"
          integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
          crossorigin="anonymous" />

    <!-- Google Fonts based on language -->
    @if (currentLanguage == "ar" || currentLanguage == "ku")
    {
        <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    }
    else
    {
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    }

    <!-- Animate.css for animations -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet" />
@*  <link rel="stylesheet" href="~/css/styles-fixed.css" />
    <link rel="stylesheet" href="~/css/footer-enhanced.css" /> *@

    <link rel="stylesheet" href="~/css/styles.css?v=1.0" />
@*     <link rel="stylesheet" href="~/css/components.css?v=3.0" />
    <link rel="stylesheet" href="~/css/advertisements.css?v=3.2" />
    <link rel="stylesheet" href="~/css/cards-enhanced.css?v=1.0" /> *@ 
        <!-- ✨ الملفات المحسّنة - يجب أن تكون أولاً -->
    <link rel="stylesheet" href="~/css/styles-fixed.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/footer-enhanced.css" asp-append-version="true" />

    <!-- الملفات الأخرى -->
    <link rel="stylesheet" href="~/css/components.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/cards-enhanced.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/advertisements.css" asp-append-version="true" />

    @await RenderSectionAsync("Styles", required: false)



    <!-- Custom CSS -->
 @*    <style>
        .btn,
        .btn-custom,
        .btn-outline-custom,
        button[type="submit"],
        button[type="button"],
        a.btn {
            /* الحجم والخط */
            font-size: 18px !important;
            font-weight: 600 !important;
            font-family: 'Cairo', sans-serif !important;
            line-height: 1.5 !important;
            /* المسافات */
            padding: 0.875rem 2rem !important;
            /* الشكل */
            border-radius: 12px !important;
            /* العرض */
            display: inline-flex !important;
            align-items: center !important;
            justify-content: center !important;
            gap: 0.75rem !important;
            /* التأثيرات */
            transition: all 0.3s ease !important;
            text-decoration: none !important;
            cursor: pointer !important;
        }

        /* زر أساسي */
        .btn-custom,
        .btn-primary {
            background: linear-gradient(135deg, #d4b616 0%, #b89f13 100%) !important;
            color: white !important;
            border: none !important;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08) !important;
        }

            .btn-custom:hover,
            .btn-primary:hover {
                background: linear-gradient(135deg, #b89f13 0%, #d4b616 100%) !important;
                color: white !important;
                transform: translateY(-3px) !important;
                box-shadow: 0 8px 24px rgba(212, 182, 22, 0.25) !important;
            }

        /* زر محيطي */
        .btn-outline-custom {
            background: transparent !important;
            border: 2px solid #d4b616 !important;
            color: #d4b616 !important;
        }

            .btn-outline-custom:hover {
                background: #d4b616 !important;
                color: white !important;
                transform: translateY(-3px) !important;
            }

        /* أحجام الأزرار */
        .btn-sm {
            padding: 0.625rem 1.25rem !important;
            font-size: 16px !important;
        }

        .btn-lg {
            padding: 1.125rem 2.5rem !important;
            font-size: 20px !important;
        }

        /* ==========================================
           2. إصلاح الخطوط فوراً
           ========================================== */

        /* توحيد الخط */
        body,
        body *,
        h1, h2, h3, h4, h5, h6,
        p, span, div, a,
        .navbar,
        .footer,
        .card,
        .form-control,
        .form-select {
            font-family: 'Cairo', -apple-system, BlinkMacSystemFont, sans-serif !important;
        }

        /* حجم الخط الأساسي */
        body {
            font-size: 18px !important;
            line-height: 1.75 !important;
        }

        /* العناوين */
        h1, .h1 {
            font-size: 48px !important;
            font-weight: 800 !important;
        }

        h2, .h2 {
            font-size: 38px !important;
            font-weight: 700 !important;
        }

        h3, .h3 {
            font-size: 30px !important;
            font-weight: 700 !important;
        }

        h4, .h4 {
            font-size: 26px !important;
            font-weight: 600 !important;
        }

        h5, .h5 {
            font-size: 22px !important;
            font-weight: 600 !important;
        }

        h6, .h6 {
            font-size: 20px !important;
            font-weight: 600 !important;
        }

        /* النصوص */
        p {
            font-size: 18px !important;
            line-height: 1.75 !important;
        }

        a {
            font-size: 18px !important;
        }

        /* ==========================================
           3. إصلاح الفوتر فوراً - سطر واحد
           ========================================== */

        .main-footer {
            background: #27272E !important;
            padding: 2rem 0 1.25rem !important;
        }

            /* محتوى الفوتر */
            .main-footer .container,
            .main-footer .footer-content {
                display: flex !important;
                justify-content: space-between !important;
                align-items: center !important;
                flex-wrap: nowrap !important;
                gap: 2rem !important;
                margin-bottom: 1.5rem !important;
            }

            /* أقسام الفوتر */
            .main-footer .footer-section {
                flex: 1 1 auto !important;
                min-width: 0 !important;
            }

            /* عناوين الفوتر */
            .main-footer h5 {
                font-size: 20px !important;
                margin-bottom: 0.75rem !important;
                color: #d4b616 !important;
            }

            /* روابط الفوتر */
            .main-footer ul {
                display: flex !important;
                flex-wrap: nowrap !important;
                gap: 1.5rem !important;
                list-style: none !important;
                padding: 0 !important;
                margin: 0 !important;
            }

                .main-footer ul li {
                    margin: 0 !important;
                }

            .main-footer a {
                color: rgba(255,255,255,0.75) !important;
                font-size: 16px !important;
                white-space: nowrap !important;
                text-decoration: none !important;
            }

                .main-footer a:hover {
                    color: #d4b616 !important;
                }

        /* سطر الحقوق */
        .footer-bottom {
            border-top: 1px solid rgba(255,255,255,0.1) !important;
            padding-top: 1.25rem !important;
            text-align: center !important;
        }

            .footer-bottom p {
                font-size: 14px !important;
                color: rgba(255,255,255,0.6) !important;
                margin: 0 !important;
            }

        /* ==========================================
           4. إصلاحات إضافية
           ========================================== */

        /* Hero Section */
        .hero-section h1 {
            font-size: 3.5rem !important;
        }

        .hero-section p {
            font-size: 1.5rem !important;
        }

        /* Navbar */
        .navbar-nav .nav-link {
            font-size: 18px !important;
            padding: 0.75rem 1.25rem !important;
        }

        .navbar-brand {
            font-size: 1.75rem !important;
        }

        /* Forms */
        .form-control,
        .form-select {
            font-size: 18px !important;
            padding: 0.875rem 1.25rem !important;
        }

        .form-label {
            font-size: 18px !important;
            font-weight: 500 !important;
        }

        /* Tables */
        .table {
            font-size: 18px !important;
        }

            .table th {
                font-size: 18px !important;
                padding: 1.25rem 1rem !important;
            }

            .table td {
                font-size: 18px !important;
                padding: 1.25rem 1rem !important;
            }

        /* Cards */
        .card-title,
        .category-content h5,
        .request-card-body h5 {
            font-size: 22px !important;
        }

        .card-text,
        .category-content p,
        .request-card-body p {
            font-size: 18px !important;
        }

        /* ==========================================
           5. تصميم متجاوب
           ========================================== */

        /* Tablet */
        @@media (max-width: 991px) {
            body

        {
            font-size: 17px !important;
        }

        .btn {
            font-size: 17px !important;
        }

        h1 {
            font-size: 42px !important;
        }

        h2 {
            font-size: 34px !important;
        }

        /* فوتر في سطرين */
        .main-footer .container,
        .main-footer .footer-content {
            flex-wrap: wrap !important;
        }

        .main-footer ul {
            flex-direction: column !important;
            gap: 0.75rem !important;
        }

        }

        /* Mobile */
        @@media (max-width: 768px) {
            body

        {
            font-size: 16px !important;
        }

        .btn {
            font-size: 16px !important;
            padding: 0.75rem 1.5rem !important;
            width: 100% !important;
        }

        h1 {
            font-size: 36px !important;
        }

        h2 {
            font-size: 28px !important;
        }

        h3 {
            font-size: 24px !important;
        }

        .hero-section h1 {
            font-size: 2rem !important;
        }

        /* فوتر في عمود */
        .main-footer .container,
        .main-footer .footer-content {
            flex-direction: column !important;
            align-items: flex-start !important;
        }

        }

        /* ==========================================
           6. تأكيدات نهائية - أعلى أولوية
           ========================================== */

        /* تأكيد الأزرار */
        button,
        a.btn,
        .btn {
            min-height: 48px !important;
            min-width: auto !important;
        }

        /* تأكيد الخطوط */
        * {
            font-family: 'Cairo', sans-serif !important;
        }

        body[dir="ltr"] * {
            font-family: 'Montserrat', sans-serif !important;
        }

        /* تأكيد الفوتر */
        .main-footer {
            width: 100% !important;
            overflow: hidden !important;
        }
    </style> *@

</head>

<body>
    <header class="main-header">
        <div class="top-bar d-none d-md-block">
            <div class="container">
                <div class="contact-info">
                    @if (!string.IsNullOrEmpty(siteSettings?.ContactPhone))
                    {
                        <a href="tel:@siteSettings.ContactPhone">
                            <i class="fas fa-phone"></i>
                            @siteSettings.ContactPhone
                        </a>
                    }
                    @if (!string.IsNullOrEmpty(siteSettings?.ContactEmail))
                    {
                        <a href="mailto:@siteSettings.ContactEmail">
                            <i class="fas fa-envelope"></i>
                            @siteSettings.ContactEmail
                        </a>
                    }
                </div>

                <div class="social-links">
                    @if (!string.IsNullOrEmpty(siteSettings?.FacebookUrl))
                    {
                        <a href="@siteSettings.FacebookUrl" target="_blank" title="Facebook">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                    }
                    @if (!string.IsNullOrEmpty(siteSettings?.InstagramUrl))
                    {
                        <a href="@siteSettings.InstagramUrl" target="_blank" title="Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                    }
                    @if (!string.IsNullOrEmpty(siteSettings?.TwitterUrl))
                    {
                        <a href="@siteSettings.TwitterUrl" target="_blank" title="Twitter">
                            <i class="fab fa-twitter"></i>
                        </a>
                    }
                    @if (!string.IsNullOrEmpty(siteSettings?.YouTubeUrl))
                    {
                        <a href="@siteSettings.YouTubeUrl" target="_blank" title="YouTube">
                            <i class="fab fa-youtube"></i>
                        </a>
                    }
                </div>
            </div>
        </div>

        <nav class="navbar navbar-expand-lg main-navbar">
            <div class="container">
                <a class="navbar-brand" href="/">
                    @if (!string.IsNullOrEmpty(siteSettings?.SiteLogo))
                    {
                        <img src="@siteSettings.SiteLogo" alt="@Localizer["SiteName"]" />
                    }
                    else
                    {
                        <i class="fas fa-store brand-icon"></i>
                    }
                    @Localizer["SiteName"]
                </a>

                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav @(direction == "rtl" ? "ms-auto" : "me-auto")">
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Index" && ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active" : "")" href="/">
                                <i class="fas fa-home @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["Home"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Requests" ? "active" : "")" href="/Requests">
                                <i class="fas fa-list @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["Requests"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Stores" ? "active" : "")" href="/Stores">
                                <i class="fas fa-store @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["Stores"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "About" ? "active" : "")" href="/Home/About">
                                <i class="fas fa-info-circle @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["About"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Contact" ? "active" : "")" href="/Home/Contact">
                                <i class="fas fa-envelope @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["Contact"]
                            </a>
                        </li>
                    </ul>

                    <ul class="navbar-nav">


                        <!-- Notifications Bell -->
                        @if (isLoggedIn)
                        {
                            <li class="nav-item">
                                @await Html.PartialAsync("_NotificationBell")
                            </li>
                        }
                        <!-- Language Selector -->
                        <li class="nav-item dropdown language-selector">
                            <a class="nav-link dropdown-toggle language-dropdown" href="#" id="languageMenu"
                               role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <span class="flag">@currentLangInfo.Flag</span>
                                <span class="d-none d-sm-inline">@currentLangInfo.NativeName</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" id="languageDropdown">
                                @foreach (var language in supportedLanguages)
                                {
                                    <li>
                                        <form method="post" action="@Url.Action("SetLanguage", "Language")" class="language-form">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="culture" value="@language.Code" />
                                            <input type="hidden" name="returnUrl" value="@(Context.Request.Path + Context.Request.QueryString)" />
                                            <button type="submit"
                                                    class="dropdown-item @(currentLanguage == language.Code ? "active" : "")"
                                                    @(currentLanguage == language.Code ? "disabled" : "")>
                                                <span class="flag">@language.Flag</span>
                                                <span>@language.NativeName</span>
                                                @if (currentLanguage == language.Code)
                                                {
                                                    <i class="fas fa-check ms-auto text-success"></i>
                                                }
                                            </button>
                                        </form>
                                    </li>
                                }
                            </ul>
                        </li>

                        <!-- User Menu -->
                        @if (isLoggedIn)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle user-dropdown" href="#" id="userMenu"
                                   role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <div class="user-avatar">@(userName?.FirstOrDefault() ?? 'U')</div>
                                    <span class="d-none d-md-inline">@userName</span>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    @if (isAdmin)
                                    {
                                        <li>
                                            <a class="dropdown-item" href="/Admin/Dashboard">
                                                <i class="fas fa-tachometer-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>
                                                @Localizer["AdminPanel"]
                                            </a>
                                        </li>
                                    }
                                    else
                                    {
                                        <li>
                                            <a class="dropdown-item" href="/Profile">
                                                <i class="fas fa-user @(direction == "rtl" ? "ms-2" : "me-2")"></i>
                                                @Localizer["Profile"]
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" href="/Requests/Create">
                                                <i class="fas fa-plus @(direction == "rtl" ? "ms-2" : "me-2")"></i>
                                                @Localizer["AddRequest"]
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" href="/Profile/MyRequests">
                                                <i class="fas fa-list-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>
                                                @Localizer["MyRequests"]
                                            </a>
                                        </li>
                                    }
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <form asp-action="Logout" asp-controller="Account" method="post">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="dropdown-item text-danger">
                                                <i class="fas fa-sign-out-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>
                                                @Localizer["Logout"]
                                            </button>
                                        </form>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Requests/Create">
                                    <i class="fas fa-plus @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                    @Localizer["AddRequest"]
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="btn btn-outline-custom" href="/Account/Login">
                                    <i class="fas fa-sign-in-alt"></i>
                                    @Localizer["Login"]
                                </a>
                            </li>
                        }
                    </ul>


            
                @*     <ul class="navbar-nav">
                        <li class="nav-item dropdown language-selector">
                            <a class="nav-link dropdown-toggle language-dropdown" href="#" id="languageMenu"
                               role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <span class="flag">@currentLangInfo.Flag</span>
                                <span class="d-none d-sm-inline">@currentLangInfo.NativeName</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" id="languageDropdown">
                                @foreach (var language in supportedLanguages)
                                {
                                    <li>
                                        <form method="post" action="@Url.Action("SetLanguage", "Language")" class="language-form">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="culture" value="@language.Code" />
                                            <input type="hidden" name="returnUrl" value="@(Context.Request.Path + Context.Request.QueryString)" />
                                            <button type="submit"
                                                    class="dropdown-item @(currentLanguage == language.Code ? "active" : "")"
                                                    @(currentLanguage == language.Code ? "disabled" : "")>
                                                <span class="flag">@language.Flag</span>
                                                <span>@language.NativeName</span>
                                                @if (currentLanguage == language.Code)
                                                {
                                                    <i class="fas fa-check ms-auto text-success"></i>
                                                }
                                            </button>
                                        </form>
                                    </li>
                                }
                            </ul>
                        </li>

                        @if (isLoggedIn)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle user-dropdown" href="#" id="userMenu" role="button" data-bs-toggle="dropdown">
                                    <div class="user-avatar">@(userName?.FirstOrDefault() ?? 'U')</div>
                                    <span class="d-none d-md-inline">@userName</span>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    @if (isAdmin)
                                    {
                                        <li><a class="dropdown-item" href="/Admin/Dashboard"><i class="fas fa-tachometer-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["AdminPanel"]</a></li>
                                    }
                                    else
                                    {
                                        <li><a class="dropdown-item" href="/Account/Profile"><i class="fas fa-user @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["Profile"]</a></li>
                                        <li><a class="dropdown-item" href="/Requests/Create"><i class="fas fa-plus @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["AddRequest"]</a></li>
                                        <li><a class="dropdown-item" href="/Requests/MyRequests"><i class="fas fa-list-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["MyRequests"]</a></li>
                                    }
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item text-danger" href="/Account/Logout"><i class="fas fa-sign-out-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["Logout"]</a></li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item"><a class="nav-link" href="/Requests/Create"><i class="fas fa-plus @(direction == "rtl" ? "ms-1" : "me-1")"></i>@Localizer["AddRequest"]</a></li>
                            <li class="nav-item">
                                <a class="btn btn-outline-custom" href="/Account/Login">
                                    <i class="fas fa-sign-in-alt"></i>
                                    @Localizer["Login"]
                                </a>
                            </li>
                        }
                    </ul> *@
                </div>
            </div>
        </nav>
    </header>

    <main role="main" class="fade-in">
        <div class="messages-container">
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="container mt-3">
                    <div class="alert alert-success alert-dismissible fade show animate__animated animate__slideInDown" role="alert">
                        <i class="fas fa-check-circle"></i>
                        <div>
                            <strong>@Localizer["Success"]!</strong>
                            @TempData["SuccessMessage"]
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            }

            @if (TempData["ErrorMessage"] != null)
            {
                <div class="container mt-3">
                    <div class="alert alert-danger alert-dismissible fade show animate__animated animate__slideInDown" role="alert">
                        <i class="fas fa-exclamation-circle"></i>
                        <div>
                            <strong>@Localizer["Error"]!</strong>
                            @TempData["ErrorMessage"]
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            }

            @if (TempData["WarningMessage"] != null)
            {
                <div class="container mt-3">
                    <div class="alert alert-warning alert-dismissible fade show animate__animated animate__slideInDown" role="alert">
                        <i class="fas fa-exclamation-triangle"></i>
                        <div>
                            <strong>تنبيه!</strong>
                            @TempData["WarningMessage"]
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            }

            @if (TempData["InfoMessage"] != null)
            {
                <div class="container mt-3">
                    <div class="alert alert-info alert-dismissible fade show animate__animated animate__slideInDown" role="alert">
                        <i class="fas fa-info-circle"></i>
                        <div>
                            <strong>معلومة!</strong>
                            @TempData["InfoMessage"]
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            }
        </div>

        @RenderBody()
    </main>

    <footer class="main-footer">
        <div class="container">
            <div class="row g-4">
                <!-- Logo and About Section -->
                <div class="col-lg-3 col-md-6 footer-section">
                    @if (!string.IsNullOrEmpty(siteSettings?.SiteLogo))
                    {
                        <img src="@siteSettings.SiteLogo" alt="@Localizer["SiteName"]" class="footer-logo" />
                    }
                    else
                    {
                        <h5>@Localizer["SiteName"]</h5>
                    }
                    <p class="mb-3">
                        @{
                            var aboutUsText = siteSettings?.GetAboutUs(currentLanguage);
                            if (!string.IsNullOrEmpty(aboutUsText))
                            {
                                @(aboutUsText.Length > 150 ? aboutUsText.Substring(0, 150) + "..." : aboutUsText)
                            }
                            else
                            {
                                @Localizer["SiteDescription"]
                            }
                        }
                    </p>
                </div>

                <!-- Social Media Links -->
                <div class="col-lg-2 col-md-6 footer-section">
                    <h5>@Localizer["SocialMedia"]</h5>
                    <ul class="list-unstyled">
                        @if (!string.IsNullOrEmpty(siteSettings?.FacebookUrl))
                        {
                            <li>
                                <a href="@siteSettings.FacebookUrl" target="_blank">
                                    <i class="fab fa-facebook"></i> @Localizer["Facebook"]
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.InstagramUrl))
                        {
                            <li>
                                <a href="@siteSettings.InstagramUrl" target="_blank">
                                    <i class="fab fa-instagram"></i> @Localizer["Instagram"]
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.TwitterUrl))
                        {
                            <li>
                                <a href="@siteSettings.TwitterUrl" target="_blank">
                                    <i class="fab fa-twitter"></i> @Localizer["Twitter"]
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.YouTubeUrl))
                        {
                            <li>
                                <a href="@siteSettings.YouTubeUrl" target="_blank">
                                    <i class="fab fa-youtube"></i> @Localizer["YouTube"]
                                </a>
                            </li>
                        }
                    </ul>
                </div>

                <!-- Contact Information -->
                <div class="col-lg-3 col-md-6 footer-section">
                    <h5>@Localizer["Contact"]</h5>
                    <ul class="list-unstyled">
                        @if (!string.IsNullOrEmpty(siteSettings?.ContactPhone))
                        {
                            <li>
                                <a href="tel:@siteSettings.ContactPhone">
                                    <i class="fas fa-phone"></i> @siteSettings.ContactPhone
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.ContactWhatsApp))
                        {
                            <li>
                                <a href="https://wa.me/@siteSettings.ContactWhatsApp.Replace("+", "")" target="_blank">
                                    <i class="fab fa-whatsapp"></i> @siteSettings.ContactWhatsApp
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.ContactEmail))
                        {
                            <li>
                                <a href="mailto:@siteSettings.ContactEmail">
                                    <i class="fas fa-envelope"></i> @siteSettings.ContactEmail
                                </a>
                            </li>
                        }
                    </ul>
                </div>

                <!-- Important Links -->
                <div class="col-lg-4 col-md-6 footer-section">
                    <h5>@Localizer["ImportantLinks"]</h5>
                    <div class="row">
                        <div class="col-6">
                            <ul class="list-unstyled">
                                <li><a href="/"><i class="fas fa-home"></i> @Localizer["Home"]</a></li>
                                <li><a href="/Requests"><i class="fas fa-list"></i> @Localizer["Requests"]</a></li>
                                <li><a href="/Stores"><i class="fas fa-store"></i> @Localizer["Stores"]</a></li>
                                <li><a href="/Home/About"><i class="fas fa-info-circle"></i> @Localizer["About"]</a></li>
                            </ul>
                        </div>
                        <div class="col-6">
                            <ul class="list-unstyled">
                                <li><a href="/Home/Contact"><i class="fas fa-envelope"></i> @Localizer["Contact"]</a></li>
                                <li><a href="/Home/Privacy"><i class="fas fa-shield-alt"></i> @Localizer["PrivacyPolicy"]</a></li>
                                <li><a href="/Home/Terms"><i class="fas fa-file-contract"></i> @Localizer["TermsOfUse"]</a></li>
                                <li><a href="/Home/IntellectualProperty"><i class="fas fa-balance-scale"></i> @Localizer["IntellectualProperty"]</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <div class="footer-bottom">
                <div class="row align-items-center">
                    <div class="col-md-12 text-center">
                        <p class="mb-0">
                            @{
                                var copyrightText = siteSettings?.GetCopyrightInfo(currentLanguage);
                                if (!string.IsNullOrEmpty(copyrightText))
                                {
                                    @copyrightText
                                }
                                else
                                {
                                    @Localizer["Copyright"]
                                }
                            }
                            <span class="@(direction == "rtl" ? "me-2 ms-2" : "mx-2")">|</span>
                            <i class="fas fa-heart text-danger"></i>
                            @if (currentLanguage == "ar")
                            {
                                <text>صُنع بحب في العراق</text>
                            }
                            else if (currentLanguage == "en")
                            {
                                <text>Made with love in Iraq</text>
                            }
                            else if (currentLanguage == "ku")
                            {
                                <text>بە خۆشەویستی دروستکراوە لە عێراق</text>
                            }
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <button id="backToTop" class="btn btn-custom position-fixed" style="bottom: 2rem; @(direction == "rtl" ? "left" : "right"): 2rem; z-index: 1000; display: none; border-radius: 50%; width: 50px; height: 50px;">
        <i class="fas fa-arrow-up"></i>
    </button>

@*     <!-- Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- language-manager -->
    <script src="~/js/language-manager.js"></script>

    <script>
        if (typeof bootstrap === 'undefined') {
            console.error('❌ Bootstrap غير محمل');
            alert('خطأ: Bootstrap غير محمل. تحقق من الاتصال بالإنترنت.');
        } else {
            console.log('✅ Bootstrap محمل بنجاح');
        }

        console.log('🌐 اللغة الحالية:', '@currentLanguage');
        console.log('📂 الاتجاه:', '@direction');
        console.log('👤 تسجيل دخول:', @isLoggedIn.ToString().ToLower());
        console.log('🔐 اسم المستخدم:', '@userName');
    </script> *@
    <!-- jQuery - يجب أن يكون أولاً -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"
            integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
            crossorigin="anonymous"></script>

    <!-- Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- language-manager -->
    <script src="~/js/language-manager.js"></script>
    <script src="~/js/advertisements.js?v=3.2"></script>
    <!-- SignalR -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.0/signalr.min.js"></script>

    <!-- Notification Manager -->
    <script src="~/js/notification-manager.js"></script>
    <script>
        // التحقق من تحميل jQuery
        if (typeof jQuery === 'undefined') {
            console.error('❌ jQuery غير محمل');
            alert('خطأ: jQuery غير محمل. تحقق من الاتصال بالإنترنت.');
        } else {
            console.log('✅ jQuery محمل بنجاح - الإصدار:', jQuery.fn.jquery);
        }

        if (typeof bootstrap === 'undefined') {
            console.error('❌ Bootstrap غير محمل');
            alert('خطأ: Bootstrap غير محمل. تحقق من الاتصال بالإنترنت.');
        } else {
            console.log('✅ Bootstrap محمل بنجاح');
        }

        console.log('🌐 اللغة الحالية:', '@currentLanguage');
        console.log('📂 الاتجاه:', '@direction');
        console.log('👤 تسجيل دخول:', @isLoggedIn.ToString().ToLower());
        console.log('🔐 اسم المستخدم:', '@userName');
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>


@* @using Microsoft.Extensions.Localization
@using ReverseMarket.Services
@using ReverseMarket.Resources
@inject IStringLocalizer<SharedResource> Localizer
@inject ILanguageService LanguageService

@{
    var currentLanguage = LanguageService.GetCurrentLanguage();
    var direction = LanguageService.GetDirection();
    var supportedLanguages = LanguageService.GetSupportedLanguages();
    var currentLangInfo = supportedLanguages.FirstOrDefault(l => l.Code == currentLanguage) ?? supportedLanguages.First();

    var siteSettings = ViewBag.SiteSettings as ReverseMarket.Models.SiteSettings;
    var isLoggedIn = Context.Session.GetString("IsLoggedIn") == "true";
    var userName = Context.Session.GetString("UserName");
    var userType = Context.Session.GetString("UserType");
}

<!DOCTYPE html>
<html lang="@currentLanguage" dir="@direction">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="@Localizer["SiteDescription"]" />
    <meta name="keywords" content="سوق عكسي, طلبات, متاجر, العراق" />
    <meta name="author" content="@Localizer["SiteName"]" />

    <title>@ViewData["Title"] - @Localizer["SiteName"]</title>

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />

    <!-- Bootstrap CSS with RTL/LTR support -->
    @if (direction == "rtl")
    {
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css"
              rel="stylesheet"
              integrity="sha384-dpuaG1suU0eT09tx5plTaGMLBsfDLzUCCUXOY2j/LSvXYuG6Bqs43ALlhIqAJVRb"
              crossorigin="anonymous">
    }
    else
    {
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
              rel="stylesheet"
              integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
              crossorigin="anonymous">
    }

    <!-- Font Awesome Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
          rel="stylesheet"
          integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
          crossorigin="anonymous" />

    <!-- Google Fonts based on language -->
    @if (currentLanguage == "ar" || currentLanguage == "ku")
    {
        <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    }
    else
    {
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
    }

    <!-- Animate.css for animations -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet" />

    <!-- Custom CSS -->
    <style>
       .btn,
.btn-custom,
.btn-outline-custom,
button[type="submit"],
button[type="button"],
a.btn {
    /* الحجم والخط */
    font-size: 18px !important;
    font-weight: 600 !important;
    font-family: 'Cairo', sans-serif !important;
    line-height: 1.5 !important;
    
    /* المسافات */
    padding: 0.875rem 2rem !important;
    
    /* الشكل */
    border-radius: 12px !important;
    
    /* العرض */
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 0.75rem !important;
    
    /* التأثيرات */
    transition: all 0.3s ease !important;
    text-decoration: none !important;
    cursor: pointer !important;
}

/* زر أساسي */
.btn-custom,
.btn-primary {
    background: linear-gradient(135deg, #d4b616 0%, #b89f13 100%) !important;
    color: white !important;
    border: none !important;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08) !important;
}

.btn-custom:hover,
.btn-primary:hover {
    background: linear-gradient(135deg, #b89f13 0%, #d4b616 100%) !important;
    color: white !important;
    transform: translateY(-3px) !important;
    box-shadow: 0 8px 24px rgba(212, 182, 22, 0.25) !important;
}

/* زر محيطي */
.btn-outline-custom {
    background: transparent !important;
    border: 2px solid #d4b616 !important;
    color: #d4b616 !important;
}

.btn-outline-custom:hover {
    background: #d4b616 !important;
    color: white !important;
    transform: translateY(-3px) !important;
}

/* أحجام الأزرار */
.btn-sm {
    padding: 0.625rem 1.25rem !important;
    font-size: 16px !important;
}

.btn-lg {
    padding: 1.125rem 2.5rem !important;
    font-size: 20px !important;
}

/* ==========================================
   2. إصلاح الخطوط فوراً
   ========================================== */

/* توحيد الخط */
body,
body *,
h1, h2, h3, h4, h5, h6,
p, span, div, a,
.navbar,
.footer,
.card,
.form-control,
.form-select {
    font-family: 'Cairo', -apple-system, BlinkMacSystemFont, sans-serif !important;
}

/* حجم الخط الأساسي */
body {
    font-size: 18px !important;
    line-height: 1.75 !important;
}

/* العناوين */
h1, .h1 {
    font-size: 48px !important;
    font-weight: 800 !important;
}

h2, .h2 {
    font-size: 38px !important;
    font-weight: 700 !important;
}

h3, .h3 {
    font-size: 30px !important;
    font-weight: 700 !important;
}

h4, .h4 {
    font-size: 26px !important;
    font-weight: 600 !important;
}

h5, .h5 {
    font-size: 22px !important;
    font-weight: 600 !important;
}

h6, .h6 {
    font-size: 20px !important;
    font-weight: 600 !important;
}

/* النصوص */
p {
    font-size: 18px !important;
    line-height: 1.75 !important;
}

a {
    font-size: 18px !important;
}

/* ==========================================
   3. إصلاح الفوتر فوراً - سطر واحد
   ========================================== */

.main-footer {
    background: #27272E !important;
    padding: 2rem 0 1.25rem !important;
}

/* محتوى الفوتر */
.main-footer .container,
.main-footer .footer-content {
    display: flex !important;
    justify-content: space-between !important;
    align-items: center !important;
    flex-wrap: nowrap !important;
    gap: 2rem !important;
    margin-bottom: 1.5rem !important;
}

/* أقسام الفوتر */
.main-footer .footer-section {
    flex: 1 1 auto !important;
    min-width: 0 !important;
}

/* عناوين الفوتر */
.main-footer h5 {
    font-size: 20px !important;
    margin-bottom: 0.75rem !important;
    color: #d4b616 !important;
}

/* روابط الفوتر */
.main-footer ul {
    display: flex !important;
    flex-wrap: nowrap !important;
    gap: 1.5rem !important;
    list-style: none !important;
    padding: 0 !important;
    margin: 0 !important;
}

.main-footer ul li {
    margin: 0 !important;
}

.main-footer a {
    color: rgba(255,255,255,0.75) !important;
    font-size: 16px !important;
    white-space: nowrap !important;
    text-decoration: none !important;
}

.main-footer a:hover {
    color: #d4b616 !important;
}

/* سطر الحقوق */
.footer-bottom {
    border-top: 1px solid rgba(255,255,255,0.1) !important;
    padding-top: 1.25rem !important;
    text-align: center !important;
}

.footer-bottom p {
    font-size: 14px !important;
    color: rgba(255,255,255,0.6) !important;
    margin: 0 !important;
}

/* ==========================================
   4. إصلاحات إضافية
   ========================================== */

/* Hero Section */
.hero-section h1 {
    font-size: 3.5rem !important;
}

.hero-section p {
    font-size: 1.5rem !important;
}

/* Navbar */
.navbar-nav .nav-link {
    font-size: 18px !important;
    padding: 0.75rem 1.25rem !important;
}

.navbar-brand {
    font-size: 1.75rem !important;
}

/* Forms */
.form-control,
.form-select {
    font-size: 18px !important;
    padding: 0.875rem 1.25rem !important;
}

.form-label {
    font-size: 18px !important;
    font-weight: 500 !important;
}

/* Tables */
.table {
    font-size: 18px !important;
}

.table th {
    font-size: 18px !important;
    padding: 1.25rem 1rem !important;
}

.table td {
    font-size: 18px !important;
    padding: 1.25rem 1rem !important;
}

/* Cards */
.card-title,
.category-content h5,
.request-card-body h5 {
    font-size: 22px !important;
}

.card-text,
.category-content p,
.request-card-body p {
    font-size: 18px !important;
}

/* ==========================================
   5. تصميم متجاوب
   ========================================== */

/* Tablet */
@media (max-width: 991px) {
    body {
        font-size: 17px !important;
    }
    
    .btn {
        font-size: 17px !important;
    }
    
    h1 { font-size: 42px !important; }
    h2 { font-size: 34px !important; }
    
    /* فوتر في سطرين */
    .main-footer .container,
    .main-footer .footer-content {
        flex-wrap: wrap !important;
    }
    
    .main-footer ul {
        flex-direction: column !important;
        gap: 0.75rem !important;
    }
}

/* Mobile */
@media (max-width: 768px) {
    body {
        font-size: 16px !important;
    }
    
    .btn {
        font-size: 16px !important;
        padding: 0.75rem 1.5rem !important;
        width: 100% !important;
    }
    
    h1 { font-size: 36px !important; }
    h2 { font-size: 28px !important; }
    h3 { font-size: 24px !important; }
    
    .hero-section h1 {
        font-size: 2rem !important;
    }
    
    /* فوتر في عمود */
    .main-footer .container,
    .main-footer .footer-content {
        flex-direction: column !important;
        align-items: flex-start !important;
    }
}

/* ==========================================
   6. تأكيدات نهائية - أعلى أولوية
   ========================================== */

/* تأكيد الأزرار */
button,
a.btn,
.btn {
    min-height: 48px !important;
    min-width: auto !important;
}

/* تأكيد الخطوط */
* {
    font-family: 'Cairo', sans-serif !important;
}

body[dir="ltr"] * {
    font-family: 'Montserrat', sans-serif !important;
}

/* تأكيد الفوتر */
.main-footer {
    width: 100% !important;
    overflow: hidden !important;
}

    </style>
</head>

<body>
    <header class="main-header">
        <div class="top-bar d-none d-md-block">
            <div class="container">
                <div class="contact-info">
                    @if (!string.IsNullOrEmpty(siteSettings?.ContactPhone))
                    {
                        <a href="tel:@siteSettings.ContactPhone">
                            <i class="fas fa-phone"></i>
                            @siteSettings.ContactPhone
                        </a>
                    }
                    @if (!string.IsNullOrEmpty(siteSettings?.ContactEmail))
                    {
                        <a href="mailto:@siteSettings.ContactEmail">
                            <i class="fas fa-envelope"></i>
                            @siteSettings.ContactEmail
                        </a>
                    }
                </div>

                <div class="social-links">
                    @if (!string.IsNullOrEmpty(siteSettings?.FacebookUrl))
                    {
                        <a href="@siteSettings.FacebookUrl" target="_blank" title="Facebook">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                    }
                    @if (!string.IsNullOrEmpty(siteSettings?.InstagramUrl))
                    {
                        <a href="@siteSettings.InstagramUrl" target="_blank" title="Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                    }
                    @if (!string.IsNullOrEmpty(siteSettings?.TwitterUrl))
                    {
                        <a href="@siteSettings.TwitterUrl" target="_blank" title="Twitter">
                            <i class="fab fa-twitter"></i>
                        </a>
                    }
                    @if (!string.IsNullOrEmpty(siteSettings?.YouTubeUrl))
                    {
                        <a href="@siteSettings.YouTubeUrl" target="_blank" title="YouTube">
                            <i class="fab fa-youtube"></i>
                        </a>
                    }
                </div>
            </div>
        </div>

        <nav class="navbar navbar-expand-lg main-navbar">
            <div class="container">
                <a class="navbar-brand" href="/">
                    @if (!string.IsNullOrEmpty(siteSettings?.SiteLogo))
                    {
                        <img src="@siteSettings.SiteLogo" alt="@Localizer["SiteName"]" />
                    }
                    else
                    {
                        <i class="fas fa-store brand-icon"></i>
                    }
                    @Localizer["SiteName"]
                </a>

                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav @(direction == "rtl" ? "ms-auto" : "me-auto")">
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Index" && ViewContext.RouteData.Values["controller"]?.ToString() == "Home" ? "active" : "")" href="/">
                                <i class="fas fa-home @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["Home"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Requests" ? "active" : "")" href="/Requests">
                                <i class="fas fa-list @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["Requests"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Stores" ? "active" : "")" href="/Stores">
                                <i class="fas fa-store @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["Stores"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "About" ? "active" : "")" href="/Home/About">
                                <i class="fas fa-info-circle @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["About"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Contact" ? "active" : "")" href="/Home/Contact">
                                <i class="fas fa-envelope @(direction == "rtl" ? "ms-1" : "me-1")"></i>
                                @Localizer["Contact"]
                            </a>
                        </li>
                    </ul>

                    <ul class="navbar-nav">
                        <li class="nav-item dropdown language-selector">
                            <a class="nav-link dropdown-toggle language-dropdown" href="#" id="languageMenu"
                               role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <span class="flag">@currentLangInfo.Flag</span>
                                <span class="d-none d-sm-inline">@currentLangInfo.NativeName</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" id="languageDropdown">
                                @foreach (var language in supportedLanguages)
                                {
                                    <li>
                                        <form method="post" action="@Url.Action("SetLanguage", "Language")" class="language-form">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="culture" value="@language.Code" />
                                            <input type="hidden" name="returnUrl" value="@(Context.Request.Path + Context.Request.QueryString)" />
                                            <button type="submit"
                                                    class="dropdown-item @(currentLanguage == language.Code ? "active" : "")"
                                                    @(currentLanguage == language.Code ? "disabled" : "")>
                                                <span class="flag">@language.Flag</span>
                                                <span>@language.NativeName</span>
                                                @if (currentLanguage == language.Code)
                                                {
                                                    <i class="fas fa-check ms-auto text-success"></i>
                                                }
                                            </button>
                                        </form>
                                    </li>
                                }
                            </ul>
                        </li>

                        @if (isLoggedIn)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle user-dropdown" href="#" id="userMenu" role="button" data-bs-toggle="dropdown">
                                    <div class="user-avatar">@(userName?.FirstOrDefault() ?? 'U')</div>
                                    <span class="d-none d-md-inline">@userName</span>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    @if (userType == "Admin")
                                    {
                                        <li><a class="dropdown-item" href="/Admin/Dashboard"><i class="fas fa-tachometer-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["AdminPanel"]</a></li>
                                    }
                                    else
                                    {
                                        <li><a class="dropdown-item" href="/Account/Profile"><i class="fas fa-user @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["Profile"]</a></li>
                                        <li><a class="dropdown-item" href="/Requests/Create"><i class="fas fa-plus @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["AddRequest"]</a></li>
                                        <li><a class="dropdown-item" href="/Requests/MyRequests"><i class="fas fa-list-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["MyRequests"]</a></li>
                                    }
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item text-danger" href="/Account/Logout"><i class="fas fa-sign-out-alt @(direction == "rtl" ? "ms-2" : "me-2")"></i>@Localizer["Logout"]</a></li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item"><a class="nav-link" href="/Requests/Create"><i class="fas fa-plus @(direction == "rtl" ? "ms-1" : "me-1")"></i>@Localizer["AddRequest"]</a></li>
                            <li class="nav-item">
                                <a class="btn btn-outline-custom" href="/Account/Login">
                                    <i class="fas fa-sign-in-alt"></i>
                                    @Localizer["Login"]
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main role="main" class="fade-in">
        <div class="messages-container">
            @if (TempData["SuccessMessage"] != null)
            {
                <div class="container mt-3">
                    <div class="alert alert-success alert-dismissible fade show animate__animated animate__slideInDown" role="alert">
                        <i class="fas fa-check-circle"></i>
                        <div>
                            <strong>@Localizer["Success"]!</strong>
                            @TempData["SuccessMessage"]
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            }

            @if (TempData["ErrorMessage"] != null)
            {
                <div class="container mt-3">
                    <div class="alert alert-danger alert-dismissible fade show animate__animated animate__slideInDown" role="alert">
                        <i class="fas fa-exclamation-circle"></i>
                        <div>
                            <strong>@Localizer["Error"]!</strong>
                            @TempData["ErrorMessage"]
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            }

            @if (TempData["WarningMessage"] != null)
            {
                <div class="container mt-3">
                    <div class="alert alert-warning alert-dismissible fade show animate__animated animate__slideInDown" role="alert">
                        <i class="fas fa-exclamation-triangle"></i>
                        <div>
                            <strong>تنبيه!</strong>
                            @TempData["WarningMessage"]
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            }

            @if (TempData["InfoMessage"] != null)
            {
                <div class="container mt-3">
                    <div class="alert alert-info alert-dismissible fade show animate__animated animate__slideInDown" role="alert">
                        <i class="fas fa-info-circle"></i>
                        <div>
                            <strong>معلومة!</strong>
                            @TempData["InfoMessage"]
                        </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                </div>
            }
        </div>

        @RenderBody()
    </main>

    <footer class="main-footer">
        <div class="container">
            <div class="row g-4">
                <!-- Logo and About Section -->
                <div class="col-lg-3 col-md-6 footer-section">
                    @if (!string.IsNullOrEmpty(siteSettings?.SiteLogo))
                    {
                        <img src="@siteSettings.SiteLogo" alt="@Localizer["SiteName"]" class="footer-logo" />
                    }
                    else
                    {
                        <h5>@Localizer["SiteName"]</h5>
                    }
                    <p class="mb-3">
                        @{
                            var aboutUsText = siteSettings?.GetAboutUs(currentLanguage);
                            if (!string.IsNullOrEmpty(aboutUsText))
                            {
                                @(aboutUsText.Length > 150 ? aboutUsText.Substring(0, 150) + "..." : aboutUsText)
                            }
                            else
                            {
                                @Localizer["SiteDescription"]
                            }
                        }
                    </p>
                </div>

                <!-- Social Media Links -->
                <div class="col-lg-2 col-md-6 footer-section">
                    <h5>@Localizer["SocialMedia"]</h5>
                    <ul class="list-unstyled">
                        @if (!string.IsNullOrEmpty(siteSettings?.FacebookUrl))
                        {
                            <li>
                                <a href="@siteSettings.FacebookUrl" target="_blank">
                                    <i class="fab fa-facebook"></i> @Localizer["Facebook"]
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.InstagramUrl))
                        {
                            <li>
                                <a href="@siteSettings.InstagramUrl" target="_blank">
                                    <i class="fab fa-instagram"></i> @Localizer["Instagram"]
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.TwitterUrl))
                        {
                            <li>
                                <a href="@siteSettings.TwitterUrl" target="_blank">
                                    <i class="fab fa-twitter"></i> @Localizer["Twitter"]
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.YouTubeUrl))
                        {
                            <li>
                                <a href="@siteSettings.YouTubeUrl" target="_blank">
                                    <i class="fab fa-youtube"></i> @Localizer["YouTube"]
                                </a>
                            </li>
                        }
                    </ul>
                </div>

                <!-- Contact Information -->
                <div class="col-lg-3 col-md-6 footer-section">
                    <h5>@Localizer["Contact"]</h5>
                    <ul class="list-unstyled">
                        @if (!string.IsNullOrEmpty(siteSettings?.ContactPhone))
                        {
                            <li>
                                <a href="tel:@siteSettings.ContactPhone">
                                    <i class="fas fa-phone"></i> @siteSettings.ContactPhone
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.ContactWhatsApp))
                        {
                            <li>
                                <a href="https://wa.me/@siteSettings.ContactWhatsApp.Replace("+", "")" target="_blank">
                                    <i class="fab fa-whatsapp"></i> @siteSettings.ContactWhatsApp
                                </a>
                            </li>
                        }
                        @if (!string.IsNullOrEmpty(siteSettings?.ContactEmail))
                        {
                            <li>
                                <a href="mailto:@siteSettings.ContactEmail">
                                    <i class="fas fa-envelope"></i> @siteSettings.ContactEmail
                                </a>
                            </li>
                        }
                    </ul>
                </div>

                <!-- Important Links -->
                <div class="col-lg-4 col-md-6 footer-section">
                    <h5>@Localizer["ImportantLinks"]</h5>
                    <div class="row">
                        <div class="col-6">
                            <ul class="list-unstyled">
                                <li><a href="/"><i class="fas fa-home"></i> @Localizer["Home"]</a></li>
                                <li><a href="/Requests"><i class="fas fa-list"></i> @Localizer["Requests"]</a></li>
                                <li><a href="/Stores"><i class="fas fa-store"></i> @Localizer["Stores"]</a></li>
                                <li><a href="/Home/About"><i class="fas fa-info-circle"></i> @Localizer["About"]</a></li>
                            </ul>
                        </div>
                        <div class="col-6">
                            <ul class="list-unstyled">
                                <li><a href="/Home/Contact"><i class="fas fa-envelope"></i> @Localizer["Contact"]</a></li>
                                <li><a href="/Home/Privacy"><i class="fas fa-shield-alt"></i> @Localizer["PrivacyPolicy"]</a></li>
                                <li><a href="/Home/Terms"><i class="fas fa-file-contract"></i> @Localizer["TermsOfUse"]</a></li>
                                <li><a href="/Home/IntellectualProperty"><i class="fas fa-balance-scale"></i> @Localizer["IntellectualProperty"]</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <div class="footer-bottom">
                <div class="row align-items-center">
                    <div class="col-md-12 text-center">
                        <p class="mb-0">
                            @{
                                var copyrightText = siteSettings?.GetCopyrightInfo(currentLanguage);
                                if (!string.IsNullOrEmpty(copyrightText))
                                {
                                    @copyrightText
                                }
                                else
                                {
                                    @Localizer["Copyright"]
                                }
                            }
                            <span class="@(direction == "rtl" ? "me-2 ms-2" : "mx-2")">|</span>
                            <i class="fas fa-heart text-danger"></i>
                            @if (currentLanguage == "ar")
                            {
                                <text>صُنع بحب في العراق</text>
                            }
                            else if (currentLanguage == "en")
                            {
                                <text>Made with love in Iraq</text>
                            }
                            else if (currentLanguage == "ku")
                            {
                                <text>بە خۆشەویستی دروستکراوە لە عێراق</text>
                            }
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <button id="backToTop" class="btn btn-custom position-fixed" style="bottom: 2rem; @(direction == "rtl" ? "left" : "right"): 2rem; z-index: 1000; display: none; border-radius: 50%; width: 50px; height: 50px;">
        <i class="fas fa-arrow-up"></i>
    </button>

    <!-- Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- language-manager -->
    <script src="~/js/language-manager.js"></script>

    <script>
        if (typeof bootstrap === 'undefined') {
            console.error('❌ Bootstrap غير محمل');
            alert('خطأ: Bootstrap غير محمل. تحقق من الاتصال بالإنترنت.');
        } else {
            console.log('✅ Bootstrap محمل بنجاح');
        }

        console.log('🌐 اللغة الحالية:', '@currentLanguage');
        console.log('📂 الاتجاه:', '@direction');
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
 *@